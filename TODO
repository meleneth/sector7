* physics
* every x frame updates (not every frame)
* shoooting
- camera follows ship
text / comsole
remove entities - fix entity death.. Right now it crashes.
    fix crash on death
bullets need lifespan - they kill the game with few ~thousand due to every 4 frame updates
collision checking
bounds checking
macros for debug logging
radar
variable update timing - per ent, only update every X frames.
allow player death
keep old entities around for 5 seconds, possibly don't call find_or_create from server on recieve?

sector has a visible_entities, but it shouldn't since only the camera knows which entities are close enough to be visible
